@using spaApp.Services
@model spaApp.Models.Customer
@inject IRepository repository
@{
    var items = repository.Customers.Select(x => new SelectListItem(x.Name, x.Id.ToString()));
}
 
<select asp-for="Id" asp-items="@items"></select>
