@using spaApp.Services
@model spaApp.Models.Provider
@{
    var items = Repository.providers.Select(x => new SelectListItem(x.Name, x.Id.ToString()));
}

<select asp-for="Id" asp-items="@items"></select>
